============================================================
File: src\test\sources\structure\continue\f4.java
Total Inconsistencies: 1
============================================================
[1] Type     : STRUCTURAL_STYLE
BeginLocation : line 34, column 6
EndLocation   : line 46, column 6
Expected : if (j == next) {
                newQueue.add(getStatus(curX, curY, (curDirection + 1) % 8));
                int newDir = (curDirection - 1) % 8;
                if (newDir < 0) newDir = 8 + newDir;
                newQueue.add(getStatus(curX, curY, (newDir) % 8));
                break;
            } else {
                j++;
            }
Actual   : if (j == next) {
                newQueue.add(getStatus(curX, curY, (curDirection + 1) % 8));
                int newDir = (curDirection - 1) % 8;
                if (newDir < 0) newDir = 8 + newDir;
                newQueue.add(getStatus(curX, curY, (newDir) % 8));
                break;
            }
            j++;
Message  : Pattern preference 'if ( $E ) { $S+ } else { $S1+ }'

