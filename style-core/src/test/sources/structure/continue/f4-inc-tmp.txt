============================================================
File: src\test\sources\structure\continue\f4.java
Total Inconsistencies: 1
============================================================
[1] Type     : STRUCTURAL_STYLE
BeginLocation : line 41, column 7
EndLocation   : line 42, column 56
Expected : if ( newDir < 0) { newDir = 8 + newDir;
							newQueue.add(getStatus(curX, curY, (newDir) % 8));
							break;
						} else { newQueue.add(getStatus(curX, curY, (newDir) % 8));
							break;
						} 
Actual   : if (newDir < 0) newDir = 8 + newDir;
							newQueue.add(getStatus(curX, curY, (newDir) % 8));
							
Message  : Pattern preference 'if ( $E ) { $S+ } else { $S1+ }'

