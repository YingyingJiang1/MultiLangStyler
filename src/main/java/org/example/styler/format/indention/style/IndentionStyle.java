package org.example.styler.format.indention.style;

import org.example.parser.common.MyParser;
import org.example.style.Style;

public class IndentionStyle extends Style {

    public IndentionStyle(MyParser parser) {
        super(parser);
        styleName = "indention";
    }
}
