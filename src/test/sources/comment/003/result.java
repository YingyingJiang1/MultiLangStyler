importjava.util.Scanner;

publicclassMain{
    publicstaticvoidmain(String[]args){
        Scannersc=newScanner(System.in);
        intD=sc.nextInt();
        intN=sc.nextInt();
        longRESULT=(long)Math.pow(100,D)*N;
        // Calculate the N-th smallest integ
                // Calculate the N-th smallest integ
        System.out.println(RESULT);
    }
}
